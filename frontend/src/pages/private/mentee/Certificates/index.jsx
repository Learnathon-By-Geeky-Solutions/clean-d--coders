import { useTitle } from "@/hooks";

const Certificates = () => {
    useTitle("Certificates");
    return (
        <div>
            Certificates
        </div>
    );
};

export default Certificates;